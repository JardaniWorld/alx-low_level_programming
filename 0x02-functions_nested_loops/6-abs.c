#include "main.h"
#include <stdio.h>

/**
 * _abs - computes absolute value of an integer
 *
 * @n: int to print
 *
 * Return: Always 0.
 */

int _abs(int n)
{

if (n < 0)
{
return (n * (-1));
}

else if (n == o)
{
return (0);
}

else
{
return (n);
}

}
